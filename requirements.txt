langchain>=0.3.0
langchain-core>=0.3.81
langchain-openai>=0.2.0
langchain-anthropic>=0.3.0
langchain-google-genai>=2.0.0
langchain-text-splitters>=0.3.9
langchain-community>=0.3.27
langgraph>=0.2.0
langsmith>=0.1.0
pydantic>=2.0.0
python-dotenv>=1.0.0
python-docx>=1.1.0
fastapi>=0.115.0
uvicorn>=0.30.0
matplotlib>=3.8.0
pandas>=2.1.0
numpy>=1.26.0,<2.1.0
seaborn>=0.13.0
Pillow>=12.1.1
requests>=2.32.0
tavily-python>=0.5.0
# RAG dependencies
# Replit's package firewall 403s the newest chromadb wheels that it hasn't scanned/approved
# yet (1.5.9 AND 1.5.8 both 403'd). Pin to the last already-approved release. The chromadb
# API we use (PersistentClient, get_or_create_collection, add/query/count,
# delete/create_collection) is unchanged across the 1.5.x line, so this is safe.
# If 1.5.7 still 403s, step down further: try <1.5.0 (latest 1.4.x), then <1.0.0 (latest 0.6.x).
chromadb>=0.6.0,<1.5.8
pymupdf>=1.24.0
# Claude Agent SDK (code generation)
claude-agent-sdk>=0.1.59
